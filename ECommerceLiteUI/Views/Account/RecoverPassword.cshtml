@using ECommerceLiteUI.Models
@model ProfileViewModel
@{
    ViewBag.Title = "RecoverPassword";
    Layout = "~/Views/Shared/_LayoutUser.cshtml";
}

<div class="row margin-vert-30">
    <div class="col-md-12 col-md-offset-3 col-sm-offset-3">
        @using (Html.BeginForm("RecoverPassword", "Account", FormMethod.Post, new { @class = "form-horizontal" }))
        {
            @Html.AntiForgeryToken()
            if (ViewBag.Result != null)
            {
                <div class="login-header margin-bottom-30 col-md-6">
                    <h4>@ViewBag.Result</h4>
                </div><br />

            }
            else
            {
                <div class="row">
                    <div class="login-header margin-bottom-30 col-md-6">
                        <h2 class="text-center">You can demand new password</h2><br />
                    </div>
                </div>
                <div class="row">
                    <div class="input-group col-md-6 margin-bottom-20">
                        <span class="input-group-addon">
                            <i class="fa fa-user"></i>
                        </span>
                        @Html.TextBoxFor(x => x.Email, new { @class = "form-control", placeholder = "Enter your email." })
                        @Html.ValidationMessageFor(x => x.Email)
                    </div><br />
                    <div class="form-group col-md-6">
                        <button type="submit" id="btnsubmit" class="btn btn-default pull-right" style="background-color:#D10024; color:white">SUBMIT</button>
                    </div>
                </div>



            }
        }
    </div>
</div>
